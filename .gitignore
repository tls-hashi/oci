# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# OS
.DS_Store

# Vault credentials and diagnostic outputs
*.tfvars.json
vault-diag.sh
*token*
*.pem
*.key

# Documentation with potentially sensitive info
| Variable Name | Value | Category | Sen.md
siteinfo.md

# Claude AI Instructions (local only - do not commit)
.claude-instructions
.agent-instructions
.agent-instructions-template
.agent-tasks-completed.jsonl

# Security scan results (local only - do not commit)
security-scans/

# Test credentials (for testing secret detection)
# testcreds.txt
test-vault-credentials.txt

# Archived documentation with legacy configuration examples
*.archived

# Embedded git repositories
2two2.me/
